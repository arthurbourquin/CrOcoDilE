# Ask the user for a number
number = float(input("Enter a number: "))

# Ask for the exponent
exponent = int(input("Enter the power: "))

# Calculate the power
result = number ** exponent

# Print the result
print(f"{number} to the power of {exponent} is {result}")
